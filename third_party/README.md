# Third-party code

`darkreader.js` is from the official `darkreader` npm package. It is vendored unchanged and loaded before `content.js` so this extension uses [Dark Reader](https://github.com/darkreader/darkreader)'s Dynamic Theme engine through its public API.

License: MIT. See `DARKREADER_LICENSE`.
